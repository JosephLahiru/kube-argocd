apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-argocd
spec:
  replicas: 4
  selector:
    matchLabels:
      app: kube-argocd
  template:
    metadata:
      labels:
        app: kube-argocd
    spec:
      containers:
        - name: kube-argocd-container
          image: kube-argocd:5099b2507c5a6063462efb5953618c2b5f0c216c
          ports:
            - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: kube-argocd-service
spec:
  selector:
    app: kube-argocd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
