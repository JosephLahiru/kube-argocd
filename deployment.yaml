apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-argocd
spec:
  replicas: 4
  selector:
    matchLabels:
      app: kube-argocd
  template:
    metadata:
      labels:
        app: kube-argocd
    spec:
      containers:
        - name: kube-argocd-container
          image: lbdevopscti/kube-argocd:b4fda6b61627c5d1c75c85f80083254c48536917
          ports:
            - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: kube-argocd-service
spec:
  selector:
    app: kube-argocd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
